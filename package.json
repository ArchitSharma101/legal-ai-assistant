{
  "name": "legal-ai-assistant",
  "version": "1.0.0",
  "description": "Legal Document Analyzer with React frontend and FastAPI backend",
  "scripts": {
    "build": "cd frontend && npm install --legacy-peer-deps && npm run build",
    "start": "cd backend && python3 -m pip install -r requirements.txt && uvicorn server:app --host 0.0.0.0 --port $PORT"
  },
  "engines": {
    "node": "18.x"
  }
}
